{% extends 'base.html.twig' %}

{% block title %}Nos produits{% endblock %}

{% block body %}

 {% include "partials/_navbar.html.twig" %}

 <div class="row">
 <div class="col-md-3">
 <h2 class="mt-3 text-center">Filtrer</h2>
 {{ form(form) }}
 </div>
 <div class="col-md-9">
 <h2 class="mt-3 text-center">Nos Produits</h2>

<div class="row product-container">
{% for product in products %}

<div class="col-md-4">
<div class="product-item text-center mt-5">
<a href={{ path('app_oneProduct', { id: product.id }) }} </a> <img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid"></a>
<h4>{{ product.name }}</h4>
<span>{{ product.subtitle }}</span><br/>
<span>{{ (product.price /100)|number_format(2) }} €</span>
</div>
</div>
{% endfor %}
</div>
 </div>
 </div>


 {% include "partials/_footer.html.twig" %}

{% endblock %}
