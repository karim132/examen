{% extends 'base.html.twig' %}

{% block title %}{{ oneProduct.name }}{% endblock %}

{% block body %}


 {% include "partials/_navbar.html.twig" %}

<main class="one-product">
 <div class="row">
 <div class="col-md-4 mt-5">
 <img src="/uploads/{{ oneProduct.illustration }}" alt="{{ oneProduct.name }}" style="width:400px;height:500px;"  class="img-fluid rounded">
 </div>

 <div class="col-md-8 my-auto">
 <h1>{{ oneProduct.name }}</h1>
 <p>{{ oneProduct.subtitle}}</p>
 <span>{{ (oneProduct.price /100)|number_format(2) }} €</span>
 <hr>
 <p>{{ oneProduct.description }}</p>
 <hr>

 <a href={{ path('cart_ajout',{ 'id': oneProduct.id})}} class="btn btn-warning">Ajouter au panier</a>
 </div>
 </div>
 </main>






 
  

    


 {% include "partials/_footer.html.twig" %}

{% endblock %}